# 西语学习 App 更新规范

本文件规定每次收到新课件后，如何把 PDF、screenshots、meeting chat text 和学习进度转换为新 Lesson，并同步更新词汇、Class Notes、Rules & Errors Quiz、Dialogue Practice、Master Review、代码接线和版本记录。

每次修改 `index.html` 前必须同时阅读：

1. 本文件：确认固定更新流程和内容标准。
2. `西语学习App问题档案.md`：确认历史问题、待修复事项、回归测试和下一检查起点。

## 一、每次更新需要的输入

### 必需输入

- 当前最新版 `index.html`。
- 本课全部课件；优先使用按 Lesson 顺序合并、页面清晰且可复制文字的单个 PDF，也可使用 screenshots。
- 本课 meeting chat text、老师课堂文字记录，或已合并到同一 PDF 的完整课堂内容。

### 正常更新不需要进度文件

- 用户每次增加新课时，不需要导出、上传或重新导入 `mi-espanol-progress.json`。
- App 必须在每次作答后立即把结果保存到浏览器，并重新计算题目状态、skill 状态和下一轮优先级。
- 更新后的 index 启动时，必须自动把浏览器中的旧进度与新版题库合并：旧 `questionId` 继续沿用原记录，新 `questionId` 自动识别为新题。
- `mi-espanol-progress.json` 只用于备份、换设备、换浏览器、恢复数据或排查进度异常，不属于日常新增课程流程。

## 二、课件读取顺序

1. 清点 PDF / screenshots 和 meeting chat text，确认没有遗漏 Lesson、遗漏页、截断内容或无法辨认的文字。
2. 按课堂顺序提取老师讲解、例句、纠错、用户回答、新词和对话。
3. 合并不同截图和聊天记录中的重复内容，但不得因为去重而删除语境差异。
4. 区分以下内容：
   - 新词和常用表达。
   - 新语法或新规则。
   - 旧知识的新用法。
   - 老师明确纠正的错误。
   - 适合转成对话练习的真实课堂内容。
5. 如果材料不足以准确重建某个模块，先在问题档案记录缺失内容并向用户说明，不自行杜撰课件事实。

## 三、A1–B2 新词筛选规则

### 收录范围

- 收录课件中出现的 A1–B2 常用词、短语、固定搭配和实用句块。
- 只收录当前 App 以前没有出现过的内容。
- 新词判断前必须扫描全部已有 Lesson 词库；如果用户提供 progress JSON，也要对照 My Words。

### 去重规则

- 同一个 lemma 的普通变位不重复作为多个词条，例如 `comer / como / comió` 通常保留原形，并把重要变位写入 Notes 或 Quiz。
- 如果某个变位本身是本课难点或高度不规则形式，可以作为复习内容，但要明确它与原形的关系。
- 相同单词的不同常用含义可以分别保留，但必须附带语境。
- 短语的意义不能从单个词直接推导时，作为完整词条收录。
- 名词尽量带冠词；形容词标注必要的性数变化；动词尽量保留 infinitivo。

### 质量要求

- 不为了增加数量收录明显超出 B2、极少使用或与本课无关的词。
- 不重复添加已经存在于 Lesson vocabulary 或 My Words 的词。
- 每个词条的释义必须符合课件语境，而不是只提供脱离语境的词典第一义。

## 四、单词复习与掌握退出规则

### 两个方向独立记录

- Spanish → English/中文：看到西语能否理解。
- English/中文 → Spanish：看到意思能否主动想出西语。
- 两个方向分别保存连续记住次数；一个方向退出不代表整个单词已完全掌握。

### 每次作答后的安排

- `Missed`：该方向连续记住次数清零，第二天再次出现。
- 第一次 `Got it`：连续记住次数变为 1，第二天再次确认。
- 第二次连续 `Got it`：该方向标记为 mastered，退出下一次常规复习。
- `Sort of` 不算完全记住，不增加连续 `Got it` 次数，也不能触发 mastered。
- 如果一个单词的两个方向都 mastered，整个单词退出常规复习。

示例：`Missed → Got it → Missed → Got it` 的最后连续次数只有 1，因此第二天仍会出现；下一次再次 `Got it` 后，该方向才退出。

### 退出后的行为

- mastered 单词或方向不能继续进入普通 daily review 的 maintenance 抽样，否则题库仍会不断膨胀。
- 不删除历史数据；允许在用户主动查看已掌握词、老师重新强调或用户手动重置时重新激活。
- 新增 Lesson 后，新词没有历史记录，必须进入下一次相应方向的复习。

### 现有进度自动迁移

- 正常更新 index 不要求用户上传或重新导入 progress JSON。
- 新版 App 启动时直接读取现有本地 `reps`、方向和复习记录，自动建立新的 mastery 状态。
- 现有算法中 `reps` 会在答错时清零、成功时累加；迁移时可用 `reps >= 2` 作为已有连续成功两次的最佳现有依据。
- 旧数据没有单独保存完整的 `Got it` 历史，因此迁移后未来的新作答必须开始保存明确的 `fullRecallStreak`、`lastQuality` 和 mastered 状态。
- 词库注册必须使用一个完整的统一来源，自动包含所有已有 Lesson；不得因新增课程后忘记修改手写数组而漏掉新课词汇。

## 五、Class Notes 更新规则

Class Notes 必须把 PDF、截图和聊天内容重新组织成能独立复习的结构化文字，而不是逐页或逐张抄写。

推荐结构：

1. 本课核心目标。
2. 新词与常用表达。
3. 语法规则。
4. 形式变化或变位表。
5. 课堂例句及完整中文意思。
6. 老师纠正和用户容易混淆的地方。
7. 与旧知识的对比。
8. 可直接用于口语或写作的句型。

要求：

- 保留课件中的所有有学习价值的信息。
- 在尽量保留全部有效内容的前提下尽量精简：合并重复解释、相同规则和重复例句，但保留不同语境、老师纠错、例外与必要对比。
- App 中 Lesson 标题、Lesson 副标题、Class Notes 主标题、副标题和章节标题统一使用英文；正文说明与完整句意可以使用中文。
- 中文原课件中的标题不能直接照搬为 App 标题；应先准确概括成简洁英文，不改变原课主题。
- 不能只写规则名称；必须说明什么时候使用、为什么使用以及与相近结构的区别。
- 西语例句要提供足以理解整句的中文解释，不让用户因为一个未翻译的新词而看不懂语法。
- 如果两个答案都可能成立，必须说明各自成立的语境，不能强行把其中一个判为绝对错误。

## 六、Rules & Errors Quiz 规则

### 题目覆盖

- 每个独立语法点或易错难点至少建立 **2 道不同题目**。
- 两道题必须测试同一规则的不同语境，不能只替换一个名词制造表面重复。
- 例如 `pretérito vs imperfecto` 至少分别覆盖：
  - 背景、状态、习惯与进行中的动作。
  - 推动故事发展的完成事件或打断动作。
- 例如现在时词干变化至少分别覆盖：
  - 常规变位。
  - `e → ie`。
  - `e → i`。
  - 必要时加入 `o → ue`、第一人称不规则及不发生词干变化的 `nosotros`。

### 题目解释

- 蓝色答案区：题干和正确答案的完整中文意思。
- 黄色解释区：判断逻辑、易错点和相近答案的语境区别。
- 不重复描述“你选了什么”。
- 老师课堂纠正过的错误优先转成 Quiz。
- 每题必须有唯一可判定答案；如果必须依赖语境，应把必要语境写进题干。

### 稳定标识

- 每个语法点使用稳定的 `skillId`。
- 每道题使用稳定的 `questionId`，例如 `L39-PRESENT-E_IE-01`。
- 不能继续只用“题干 + 答案”的文字 hash 作为长期身份；否则以后修改翻译或措辞会让已保存的学习进度丢失。

## 七、Dialogue Practice 更新规则

- 对话必须使用本课真实出现的词汇、语法和课堂主题。
- 覆盖理解、回答、追问和自由表达，不只是朗读例句。
- 提供西语问题、辅助翻译、参考回答及必要的语法提醒。
- 参考回答应自然、符合用户当前 A1–A2 向上过渡的水平，同时适量复用 A1–B2 新词。
- 不为制造对话而加入课件中没有依据的核心语法。

## 八、Master Review 题库规则

### 题库覆盖

- 每次新增 Lesson 后必须同步更新 `MASTER_REVIEW_BANK`。
- 每个独立语法点或难点至少有 2 道不同题目。
- 新增题目必须带稳定的 `skillId`、`questionId`、`lesson`、`topic` 和 `difficulty`。
- Master Review 重点复习语法选择、易错对比、老师纠错和高价值句型；不需要把所有普通词汇机械转成选择题。

### 动态题量

- 不再固定为 20 题。
- 一个复习 cycle 中，每个被选中的 active skill 至少出现 2 道不同题目。
- 本次题量根据“新规则 + 未掌握规则 + 到期规则 + 最近错题”动态计算。
- 如果一次需要的题目过多，可以拆成连续的 Part A / Part B；完成全部部分才算完成当前 review cycle，不能因为固定题数而漏掉某个 active skill。
- 新加入题库且没有历史记录的题目属于 `new`，必须进入下一次 Master Review，不能只给一个较高随机权重后仍有可能抽不到。

### 优先级

从高到低优先考虑：

1. 本课新增且尚未测试的规则；这些题为强制入选，不参与可能落选的随机抽取。
2. 最近答错或连续正确数归零的规则。
3. 已到复习日期的规则。
4. 从未做过或题目覆盖不足的规则。
5. 最近几课但尚未掌握的规则。
6. 已掌握规则退出常规复习队列。

“最近课程”应按与当前最高 Lesson 的距离渐进计算，不能使用类似 `lesson >= 33` 的永久硬编码；Lesson 38 应比 Lesson 34 获得更高的新近权重。

加权用于决定哪些 active skills 先进入本轮；一旦某个 skill 被选中，必须保证它的两道代表题都进入本 review cycle。

### 新旧题自动合并示例

原题库有 A、B、C、D：

- A 已按规则连续掌握，因此从下一次常规队列退出。
- B、C、D 保留各自历史正确率、错误次数、到期时间和优先级。
- 更新 index 后新增 E；E 没有历史记录，自动标记为 `new`，下一次必须出现。
- 系统重新计算 B、C、D、E 的顺序；E 强制入选，其余题根据错题、到期、覆盖不足和新近程度排列。
- 更新 index 不能清空 B、C、D 的旧记录，也不需要用户重新导入进度。

## 九、掌握判定与退出机制

### 单题退出

- 同一个 `questionId` 在两个独立出现机会中均第一次作答正确后，该题可以退出下一次常规队列。
- 题目退出不等于整个语法点已经掌握；同一 `skillId` 下其他代表题仍需满足各自条件。
- 如果该题以后因长期检查、相关新课或重新激活而再次答错，应恢复为 active。

### 完全掌握

一个 skill 满足以下全部条件后标记为 `mastered`：

1. 至少存在 2 道不同代表题。
2. 两道代表题各自在第一次作答时答对 2 次。
3. 共计至少 4 次正确。
4. 正确记录至少跨 2 个独立复习场次，避免只记住同一次练习中的答案。
5. 最近用于判定的 4 次作答中没有错误。

### 掌握后的行为

- `mastered` skill 不出现在下一次常规 Master Review 中。
- 不删除题目和历史数据，避免以后无法恢复进度。
- 以下情况可以重新激活：
  - 用户后来在相关题目或新课中再次答错。
  - 老师在新课中再次纠正同一规则。
  - 该规则出现了更高一级的新用法，需要扩展原 skill 或建立新的 skill。
- 可在较长间隔后进行低频长期检查，但不能占用下一次常规复习的主要题量。

## 十、学习进度与“记忆”

### 唯一的日常流程：App 自动读取并更新

- 每次 Master Review 作答后，App 立即更新浏览器本地存储中的题目级和 skill 级进度。
- 下一题、下一轮练习以及新版 index 启动时，都直接读取同一份本地进度并重新计算队列。
- 新版 index 不应删除已掌握题目，也不应因为题目措辞变化重置进度。
- 新加入的 `questionId` 自动视为未测试；旧的稳定 ID 继续沿用原记录。
- 新题必须进入下一次复习；已退出题不应因为更新 index 而重新出现。
- 更新 index 时必须保留原 localStorage key，必要的数据结构升级使用 migration，不能用新 key 绕过旧进度。

### Progress JSON 的用途

正常更新 Lesson 不需要用户提供或导入 progress JSON。它只用于：

- 备份和恢复。
- 换设备或换浏览器。
- 本地数据丢失后的恢复。
- 用户发现排序或掌握状态异常时，提供给助手排查具体记录。

导出的 JSON 必须包含：

- 普通词汇 spaced-repetition 数据。
- My Words。
- Master Review 每题的正确、错误、连续正确、最后作答和到期时间。
- 每个 `skillId` 的覆盖次数、跨场次正确记录、掌握状态和重新激活状态。
- 数据结构版本 `schemaVersion` 和导出时间。

当前 `index v44.1` 继续使用 schema v3；以后升级 schema 时仍须兼容导入 v1、v2 和 v3，缺少新字段的旧备份不得清空设备上已有的新数据。

导入旧版本进度时必须向后兼容，不能清空现有学习记录。

## 十一、新 Lesson 代码接入清单

新增课程不能只增加首页卡片。至少检查：

1. 首页 Lesson 卡片和四个练习入口。
2. Vocabulary 数据集。
3. Rules & Errors Quiz 数据集。
4. Dialogue 数据集。
5. Class Notes 页面。
6. 首次启动模式注册。
7. 页面导航和正确标题。
8. 进度恢复数据池。
9. 全课程词汇去重注册表。
10. Master Review 题库和 skill 元数据。
11. 进度 export / import 兼容性。
12. 页脚版本号、日期和改动摘要。

## 十二、交付前验证

- 冷启动直接进入新 Lesson 的四个模式。
- 从上一课切换到新课，不残留旧标题、旧题库或旧进度。
- 新词没有与旧 Lesson 或 My Words 重复。
- 每个新语法点至少有两道不同 Quiz 和两道 Master Review 代表题。
- Master Review 题量能覆盖本轮全部 active skills。
- 已掌握 skill 不进入下一次常规复习。
- 答错 skill 会重新提高优先级。
- Export 后的 JSON 包含 Master Review 和 skill mastery；重新 Import 后结果一致。
- 历史问题档案中的所有回归测试通过。
- 更新问题档案中的状态、确认范围和下一检查起点。

## 十三、PWA 发布、缓存与数据安全

### 每次版本更新必须同步

- `index.html` 中的 `APP_VERSION`、首页版本说明和页脚版本号。
- `sw.js` 中的 `APP_VERSION` 与版本化 cache name。
- 问题档案中的当前版本、日期、修复状态和验证结果。
- 以上版本必须完全一致；不得只更新 index 而继续发布旧 `sw.js`。

### 发布文件与稳定身份

- `index.html`、`sw.js`、`manifest.json` 和 manifest / HTML 引用的图标必须发布在预期的同一稳定目录。
- manifest `id`、`start_url`、`scope` 和发布 origin 一经确定不得随版本改变。
- 当前 manifest 使用 `id: ./index.html`、`start_url: ./index.html`、`scope: ./`。`id` 与旧版未显式填写时由 `start_url` 形成的有效身份一致，不能改成 `./` 或另一文件名。
- 如果发布 origin 或目录必须变化，按新 App 处理，并在迁移前要求 Export / Import；不能假设新 origin 能读取旧 localStorage。

### Service Worker 更新规则

- HTML navigation 必须 network-first，并提供当前版本 app shell 的离线回退；不得用永久 cache-first 返回旧 index。
- 静态资源必须进入当前版本缓存。每次会影响离线内容的发布都要递增 cache version。
- 激活时只删除本 App 自己前缀下的旧版本缓存，不得清空 localStorage、IndexedDB 或其他应用缓存。
- 预缓存中的单个可选资源失败不能阻止整个新版 worker 安装。
- 新 worker 默认进入 waiting；不得在 `install` 中无条件 `skipWaiting()` 覆盖正在进行的学习 session。
- index 必须检测 `registration.waiting`、`updatefound`、重新联网和重新回到前台，并显示清晰的 `New version available / Update now`。
- 只有用户确认后才保存当前 session、向 waiting worker 发送 `SKIP_WAITING`，并在 `controllerchange` 后刷新一次。

### 学习数据保护

- Service Worker 和 cache version 变化不得修改或删除任何学习进度 localStorage key。
- App 可调用 `navigator.storage.persisted()` / `persist()` 并显示结果，但 persistent storage 仍不能替代 JSON 备份。
- iPhone 删除主屏幕 App、清除 Safari 网站数据、系统存储驱逐或更换 origin 都可能导致本地进度不可用；卸载或迁移前必须重新 Export 最新 schema JSON。
- PWA 更新本身不要求 Export / Import；正常更新应直接保留同 origin 的现有进度。

### 发布验收

- 先在本地或 staging HTTPS 环境验证首次安装、waiting worker、Update now、`controllerchange` 单次刷新、旧缓存清理与离线启动。
- 用 localStorage 哨兵和完整 Progress JSON 对比更新前后数据，确认普通词汇、Master Review、skill mastery、cycle、My Words、streak 和未完成 session 均保留。
- 实际发布时检查 `sw.js`、manifest、index 和图标均返回成功，且服务器/CDN 没有长期缓存旧 `sw.js` 或 index。
- 在真实 iPhone 主屏幕 App 上至少完成一次“旧版打开 → 检测新版 → Update now → 页脚确认新版本 → 重新 Export”的端到端测试。
- 多窗口、断网启动、恢复联网和关闭后重开不能长期停留在错误版本。
